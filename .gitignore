# IDEs
.idea/
.vscode/
*.iml

# Kotlin / Build
.gradle/
.kotlin/
build/
!gradle/wrapper/gradle-wrapper.jar
/local.properties
.externalNativeBuild
.cxx
*.hprof
.attach_pid*

# Android
captures/
capture/

# macOS
.DS_Store

# Log files
*.log

# Kotlin
/bin/

# Crashlytics
com_crashlytics_export_strings.xml
crashlytics.properties
crashlytics-build.properties
fabric.properties

# Firebase
# NOTE: composeApp/google-services.json is currently *tracked* (committed in edb33c2) and CI
# needs it to build, so it is not ignored here. It is client config rather than a true secret —
# the API key in it is restricted by package name + SHA-1 — but see ROADMAP.md Phase 6 for
# moving it to a CI secret. iOS config is local-only:
iosApp/GoogleService-Info.plist

# Signing — never commit a keystore or its passwords
keystore.properties
*.jks
*.keystore

# Secrets
secrets.properties
.env

# Local agent/tooling workspace (machine-specific paths)
.claude/
