package com.budgetmaster.auth.domain

/**
 * Disabled until the Google Sign-In SDK (`GIDSignIn`) is added to `iosApp` from Xcode and
 * the reversed-client-id URL scheme is registered — that setup requires macOS.
 */
actual val isGoogleSignInSupported: Boolean = false
